# Kept in sync manually with the version in pyproject.toml and
# electron/package.json on release - can't be read from either at runtime
# since neither ships inside the PyInstaller-frozen backend.
__version__ = "1.0.0-beta.2"
